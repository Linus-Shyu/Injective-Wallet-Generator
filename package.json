{
  "name": "rust-vue-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:wasm && vue-tsc --noEmit && vite build",
    "build:wasm": "cd wasm && wasm-pack build --target bundler -d pkg",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "injective_wallet": "file:./wasm/pkg",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0",
    "vue-tsc": "^2.1.10"
  }
}
